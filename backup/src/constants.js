export const API_BASE_URL = "http://127.0.0.1:8000";

// For the first frontend version we keep the auth layer simple and dependency-light.
// Flip this to true once the FastAPI login/register endpoints are ready.
export const USE_API_AUTH = false;

